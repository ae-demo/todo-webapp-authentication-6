{
  "name": "todo-api",
  "type": "service",
  "version": "0.1.0",
  "language": "Ballerina",
  "buildpack": "docker",
  "appPath": "todo-api",
  "entrypoint": "deployment/service",
  "exposure": "intranet",
  "dependencies": [
    { "kind": "platform-resource", "name": "todo-db", "resourceType": "postgres-cnpg" },
    { "kind": "platform-resource", "name": "user-auth", "resourceType": "thunder-app" }
  ],
  "description": "Owns to-do items: create, list, update (title/completion), and delete, each scoped to the signed-in user's own rows via the gateway's signed assertion. Persists items in the todo-db Postgres database. Does not serve any UI, and does not expose any cross-user or admin view.",
  "stories": [2, 3, 4, 5, 6, 7],
  "skillsPinned": ["openapi-conventions", "ballerina", "api-management", "thunder-authentication"]
}
